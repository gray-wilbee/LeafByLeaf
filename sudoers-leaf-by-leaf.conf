# Drop this file at /etc/sudoers.d/leaf-by-leaf
# Allows the 'will' user to restart the leaf-by-leaf service without a password
# (required for the deploy webhook to work)
will ALL=(ALL) NOPASSWD: /bin/systemctl restart leaf-by-leaf
